// .supabase-removal-checklist.md

# Supabase Migration Cleanup - Admin Auth Focus

## CRITICAL for Admin Auth (migrate NOW)
- [x] RegisterForm.tsx — DONE (now uses fetch)
- [x] Login.tsx — DONE (now uses fetch)
- [x] Dashboard — DONE (shows admin data from localStorage)
- [ ] lib/supabase.ts — Can be deleted (no admin routes use it)
- [ ] hooks/useSupabaseAuth.ts — Can be deleted (not used by admin forms)
- [ ] hooks/useSupabaseUser.ts — Can be deleted (not used by admin)

## Files still using Supabase (can be migrated progressively)

### Frontend - Can be deferred (non-admin)
- src/services/optimizedNewsfeedService.ts — Uses Supabase for newsfeed
- src/lib/uploadProfileFile.ts — Uses Supabase for candidate file upload
- src/lib/headers.ts — Uses Supabase for general user auth
- src/hooks/useGoogleAuth.ts — Uses Supabase OAuth
- src/app/auth/callback/route.ts — Supabase OAuth callback
- src/integrations/supabase/types.ts — TypeScript definitions (can delete)

### Backend - Can be deferred (webhooks/notifications)
- backend/src/routes/webhooks.ts — Supabase webhook handlers
- backend/src/routes/jobWebhook.ts — Job webhook
- backend/src/routes/webhook-microservices.ts — Microservices webhook
- backend/src/routes/microservices.ts — Microservices (some commented Supabase)
- backend/src/services/notificationQueue.ts — Uses Supabase profiles table
- backend/src/services/microserviceQueues.ts — Uses Supabase
- backend/src/integrations/socketio.ts — Uses Supabase
- backend/src/utils/socket.ts — Uses Supabase for messages
- backend/src/services/pushNotificationService.ts — Uses Supabase

### Other
- TEST_VALIDATION_SUITE.ts — Test file (can delete)

## Action Plan

### Phase 1: DELETE (safe for admin auth)
1. src/lib/supabase.ts
2. src/hooks/useSupabaseAuth.ts (replace with simple SessionContext if needed)
3. src/hooks/useSupabaseUser.ts
4. src/integrations/supabase/types.ts
5. Remove Supabase imports from src/lib/headers.ts

### Phase 2: DEFER (non-admin functionality)
- Keep webhook/microservices files for now
- Gradually migrate newsfeed, candidates, companies to PostgreSQL backend

### Phase 3: Backend cleanup (after frontend stabilizes)
- Migrate webhooks to PostgreSQL-only
- Remove all createClient imports from backend

## Implementation Notes
- Admin auth now uses: fetch/axios → Node.js backend → PostgreSQL
- Admin data stored in: localStorage + PostgreSQL admins table
- No email verification required for in-app login (but token-based for email validation)
