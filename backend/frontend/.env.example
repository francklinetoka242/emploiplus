VITE_API_URL=https://emploiplus-group.com
# When set to true the frontend will continue using local mock data
# instead of querying the backend. useful for building a fully static export.
VITE_USE_STATIC=false

# Example frontend environment variables for deployment.
# Copy to .env or set in your CI/CD/deployment environment.