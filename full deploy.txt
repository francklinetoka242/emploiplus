lancement 

sudo /home/emploiplus-group.com/full-deploy.sh

full-deploy.sh pour qu'il m'envoie un log résumé dans un fichier deploy.log à chaque exécution.


⚠️ Une petite astuce pour tes prochaines sessions

Puisque tu as eu un Broken pipe (déconnexion SSH), si jamais tu lances un build qui dure longtemps et que tu as peur que ta connexion coupe, tu peux utiliser cette commande :

Bash
nohup sudo /home/emploiplus-group.com/full-deploy.sh &







l'un regarde vers la Base de données (Kanel), l'autre regarde vers l'Utilisateur (Zod).



../build-frontend.sh && ../build-backend.sh
